import DateBox from "./DateBox";
import HeaderClient from "./HeaderClient";

const Header = () => {
  return (
    <header className="w-full flex flex-row justify-between px-6 py-4">
      <HeaderClient />
    </header>
  );
};

export default Header;
